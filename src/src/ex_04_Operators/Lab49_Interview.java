package src.ex_04_Operators;

public class Lab49_Interview {

    static void main() {
        long l = 100l; //Byte, Bits ->?
        //Byte, Bits-> 8 Bytes, 64 Bits

        String name = "Mrityunjoy";

        // To calculate bytes from bits, divide the number of bits by 8, since 1 byte equals 8 bits.
        // Example: 100 bits is 100 / 8 = 12.5 bytes (often rounded down to 12 bytes in integer contexts).

        //Formula
        //Bits ÷ 8 = Bytes
        //Or, Bytes = Bits / 8

        // long bits = 100;
        //long bytes = bits / 8;  // Integer division: 12
        //System.out.println(bytes);  // Outputs: 12
    }
}
